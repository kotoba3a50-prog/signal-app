<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <title>Окно сигналов</title>
    <!-- Скрипт Telegram WebApp -->
    <script src="https://telegram.org/js/telegram-web-app.js"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        :root {
            --bg-main: #020617;
            --bg-card: rgba(15, 23, 42, 0.92);
            --accent: #22c55e;
            --accent-soft: rgba(34, 197, 94, 0.15);
            --danger: #fb7185;
            --text-main: #e5e7eb;
            --text-muted: #9ca3af;
            --outline: #1f2937;
        }

        * {
            box-sizing: border-box;
        }

        body {
            font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            margin: 0;
            min-height: 100vh;
            padding: 16px 14px 22px;
            background:
                radial-gradient(circle at 0% 0%, rgba(34, 197, 94, 0.2), transparent 60%),
                radial-gradient(circle at 100% 100%, rgba(56, 189, 248, 0.2), transparent 60%),
                var(--bg-main);
            color: var(--text-main);
        }

        .shell {
            max-width: 480px;
            margin: 0 auto;
        }

        .topbar {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 14px;
        }

        .app-title {
            font-size: 18px;
            font-weight: 700;
            letter-spacing: 0.03em;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .app-title span.logo-dot {
            width: 10px;
            height: 10px;
            border-radius: 999px;
            background: radial-gradient(circle, #22c55e, transparent 60%);
            box-shadow: 0 0 12px rgba(34, 197, 94, 0.9);
        }

        .user-pill {
            padding: 4px 8px;
            border-radius: 999px;
            border: 1px solid rgba(148, 163, 184, 0.35);
            font-size: 11px;
            color: var(--text-muted);
            max-width: 55%;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .card {
            background: var(--bg-card);
            border-radius: 20px;
            padding: 16px 14px 14px;
            box-shadow:
                0 18px 45px rgba(15, 23, 42, 0.9),
                0 0 0 1px rgba(148, 163, 184, 0.18);
            backdrop-filter: blur(12px);
        }

        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 10px;
        }

        .card-header-title {
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            color: var(--text-muted);
        }

        .badge-live {
            font-size: 11px;
            padding: 3px 8px;
            border-radius: 999px;
            background: rgba(22, 163, 74, 0.16);
            color: #bbf7d0;
            display: inline-flex;
            align-items: center;
            gap: 4px;
        }

        .badge-live-dot {
            width: 6px;
            height: 6px;
            border-radius: 999px;
            background: #22c55e;
            box-shadow: 0 0 6px rgba(34, 197, 94, 0.9);
        }

        .field-group {
            margin-bottom: 10px;
        }

        label {
            font-size: 12px;
            color: var(--text-muted);
            display: block;
            margin-bottom: 4px;
        }

        .field-row {
            display: flex;
            gap: 8px;
        }

        /* Селекты: тёмные, без белых блоков */

        select {
            flex: 1;
            padding: 9px 11px;
            border-radius: 12px;
            border: 1px solid rgba(55, 65, 81, 0.95);
            background-color: #020617;
            background-image:
                radial-gradient(circle at 0 0, rgba(15, 23, 42, 0.9), #020617);
            color: var(--text-main);
            font-size: 13px;
            appearance: none;
            outline: none;
        }

        select:focus {
            border-color: rgba(34, 197, 94, 0.8);
            box-shadow: 0 0 0 1px rgba(34, 197, 94, 0.4);
        }

        select option {
            background-color: #020617;
            color: var(--text-main);
        }

        .pill-row {
            display: flex;
            flex-wrap: wrap;
            gap: 6px;
            margin-top: 2px;
        }

        .pill {
            padding: 4px 8px;
            border-radius: 999px;
            background: rgba(15, 23, 42, 0.95);
            font-size: 11px;
            color: var(--text-muted);
            border: 1px solid rgba(31, 41, 55, 0.9);
        }

        /* Режимы торговли */

        .mode-toggle {
            display: inline-flex;
            padding: 3px;
            border-radius: 999px;
            background: rgba(15, 23, 42, 0.95);
            border: 1px solid rgba(31, 41, 55, 0.9);
            gap: 4px;
        }

        .mode-btn {
            border: none;
            border-radius: 999px;
            padding: 5px 10px;
            font-size: 11px;
            cursor: pointer;
            background: transparent;
            color: var(--text-muted);
        }

        .mode-btn.active {
            background: linear-gradient(135deg, #22c55e, #4ade80);
            color: #022c22;
            box-shadow: 0 0 0 1px rgba(34, 197, 94, 0.7);
        }

        #btn-start {
            width: 100%;
            margin-top: 6px;
            padding: 11px 14px;
            border-radius: 999px;
            border: none;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            background: linear-gradient(135deg, #22c55e, #4ade80);
            color: #022c22;
            box-shadow: 0 12px 30px rgba(34, 197, 94, 0.45);
            transition: transform 0.08s ease-out, box-shadow 0.08s ease-out, opacity 0.2s;
        }

        #btn-start:active {
            transform: translateY(1px) scale(0.99);
            box-shadow: 0 6px 18px rgba(34, 197, 94, 0.35);
        }

        #btn-start:disabled {
            opacity: 0.6;
            cursor: default;
            box-shadow: 0 6px 16px rgba(15, 23, 42, 0.8);
        }

        .result-block {
            margin-top: 10px;
            border-radius: 16px;
            padding: 12px 10px 10px;
            background: radial-gradient(circle at 0 0, rgba(8, 47, 73, 0.9), rgba(15, 23, 42, 0.96));
            border: 1px solid rgba(51, 65, 85, 0.9);
            display: none;
        }

        .result-top {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 6px;
            font-size: 12px;
        }

        .result-asset {
            font-weight: 600;
        }

        .result-expiry {
            color: var(--text-muted);
        }

        .result-main {
            display: flex;
            align-items: center;
            justify-content: space-between;
            gap: 10px;
        }

        .direction-block {
            flex: 1.1;
        }

        .direction-label {
            font-size: 11px;
            color: var(--text-muted);
            margin-bottom: 2px;
        }

        .direction-value {
            font-size: 24px;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .direction-value span.arrow {
            font-size: 20px;
        }

        .direction-up {
            color: #4ade80;
        }

        .direction-down {
            color: #fb7185;
        }

        .timer-shell {
            flex: 0.9;
            display: flex;
            justify-content: flex-end;
        }

        .timer-circle {
            width: 82px;
            height: 82px;
            border-radius: 999px;
            border: 2px solid rgba(148, 163, 184, 0.45);
            background:
                radial-gradient(circle at 30% 0%, rgba(56, 189, 248, 0.2), transparent 55%),
                radial-gradient(circle at 0 100%, rgba(34, 197, 94, 0.25), transparent 60%),
                rgba(15, 23, 42, 0.98);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            box-shadow: 0 10px 30px rgba(15, 23, 42, 0.9);
        }

        .timer-label {
            font-size: 10px;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.12em;
        }

        .timer-value {
            font-size: 24px;
            font-weight: 700;
            margin-top: 2px;
        }

        .progress-bar {
            margin-top: 8px;
            width: 100%;
            height: 6px;
            border-radius: 999px;
            background: rgba(15, 23, 42, 0.9);
            overflow: hidden;
            border: 1px solid rgba(31, 41, 55, 0.9);
        }

        .progress-inner {
            height: 100%;
            width: 0;
            border-radius: inherit;
            background: linear-gradient(90deg, #22c55e, #a855f7);
            transition: width 0.9s linear;
        }

        .hint {
            font-size: 11px;
            color: var(--text-muted);
            margin-top: 6px;
            text-align: center;
        }

        .hint strong {
            color: #e5e7eb;
        }

        /* История сигналов */

        .history-block {
            margin-top: 10px;
            padding-top: 8px;
            border-top: 1px dashed rgba(51, 65, 85, 0.7);
        }

        .history-title {
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 0.12em;
            color: var(--text-muted);
            margin-bottom: 4px;
        }

        .history-list {
            font-size: 11px;
            color: var(--text-muted);
            max-height: 130px;
            overflow-y: auto;
        }

        .history-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 4px 0;
        }

        .history-left {
            display: flex;
            flex-direction: column;
        }

        .history-asset {
            font-weight: 600;
            color: var(--text-main);
        }

        .history-expiry {
            font-size: 10px;
        }

        .history-right {
            display: flex;
            align-items: center;
            gap: 4px;
            font-size: 11px;
        }

        .history-time {
            color: var(--text-muted);
        }

        .history-dir-up {
            color: #4ade80;
        }

        .history-dir-down {
            color: #fb7185;
        }

        @media (max-width: 360px) {
            .result-main {
                flex-direction: column;
                align-items: stretch;
            }
            .timer-shell {
                justify-content: flex-start;
            }
        }
    </style>
</head>
<body>
<div class="shell">
    <div class="topbar">
        <div class="app-title">
            <span class="logo-dot"></span>
            <span>AISIGNAL</span>
        </div>
        <div class="user-pill" id="user-pill">
            Загрузка профиля…
        </div>
    </div>

    <div class="card">
        <div class="card-header">
            <div class="card-header-title">Сигнальное окно</div>
            <div class="badge-live">
                <span class="badge-live-dot"></span>
                LIVE
            </div>
        </div>

        <!-- Режим торговли -->
        <div class="field-group">
            <label>Режим торговли</label>
            <div class="mode-toggle" id="mode-toggle">
                <button type="button" class="mode-btn active" data-mode="calm">Спокойный</button>
                <button type="button" class="mode-btn" data-mode="aggressive">Агрессивный</button>
            </div>
            <div class="pill-row">
                <div class="pill" id="mode-hint">
                    Спокойный режим — больше времени на решение, меньше шума.
                </div>
            </div>
        </div>

        <!-- Монеты / активы -->
        <div class="field-group">
            <label for="asset">Монета / актив</label>
            <div class="field-row">
                <select id="asset">
                    <!-- Forex -->
                    <option value="EURUSD">EURUSD · EUR / USD</option>
                    <option value="GBPUSD">GBPUSD · GBP / USD</option>
                    <option value="USDJPY">USDJPY · USD / JPY</option>
                    <option value="USDCHF">USDCHF · USD / CHF</option>
                    <option value="USDCAD">USDCAD · USD / CAD</option>
                    <option value="AUDUSD">AUDUSD · AUD / USD</option>
                    <option value="NZDUSD">NZDUSD · NZD / USD</option>
                    <option value="EURJPY">EURJPY · EUR / JPY</option>
                    <option value="EURGBP">EURGBP · EUR / GBP</option>
                    <option value="EURCHF">EURCHF · EUR / CHF</option>
                    <option value="GBPJPY">GBPJPY · GBP / JPY</option>
                    <!-- Metals -->
                    <option value="XAUUSD">XAUUSD · Золото</option>
                    <option value="XAGUSD">XAGUSD · Серебро</option>
                    <!-- Crypto -->
                    <option value="BTCUSDT">BTCUSDT · Bitcoin</option>
                    <option value="ETHUSDT">ETHUSDT · Ethereum</option>
                    <option value="BNBUSDT">BNBUSDT · BNB</option>
                    <option value="SOLUSDT">SOLUSDT · Solana</option>
                    <option value="XRPUSDT">XRPUSDT · XRP</option>
                    <option value="LTCUSDT">LTCUSDT · Litecoin</option>
                    <option value="DOGEUSDT">DOGEUSDT · Doge</option>
                    <option value="TONUSDT">TONUSDT · Toncoin</option>
                </select>
            </div>
            <div class="pill-row">
                <div class="pill">Выбери инструмент, по которому будешь открывать сделку у брокера.</div>
            </div>
        </div>

        <!-- Время экспирации -->
        <div class="field-group">
            <label for="expiry">Время экспирации</label>
            <div class="field-row">
                <select id="expiry">
                    <option value="5s"  data-seconds="5">5 секунд</option>
                    <option value="10s" data-seconds="10">10 секунд</option>
                    <option value="15s" data-seconds="15">15 секунд</option>
                    <option value="30s" data-seconds="30">30 секунд</option>
                    <option value="45s" data-seconds="45">45 секунд</option>
                    <option value="1m"  data-seconds="60">1 минута</option>
                    <option value="2m"  data-seconds="120">2 минуты</option>
                    <option value="3m"  data-seconds="180">3 минуты</option>
                    <option value="5m"  data-seconds="300">5 минут</option>
                    <option value="10m" data-seconds="600">10 минут</option>
                    <option value="15m" data-seconds="900">15 минут</option>
                </select>
            </div>
            <div class="pill-row">
                <div class="pill">Короткое время — больше динамики, длинное — более размеренная торговля.</div>
            </div>
        </div>

        <button id="btn-start">Получить прогноз и запустить таймер</button>

        <div class="result-block" id="signal-block">
            <div class="result-top">
                <div class="result-asset" id="signal-asset"></div>
                <div class="result-expiry" id="signal-expiry"></div>
            </div>

            <div class="result-main">
                <div class="direction-block">
                    <div class="direction-label">Прогноз по направлению</div>
                    <div class="direction-value" id="signal-direction"></div>
                </div>

                <div class="timer-shell">
                    <div class="timer-circle">
                        <div class="timer-label">таймер</div>
                        <div class="timer-value" id="timer">0</div>
                    </div>
                </div>
            </div>

            <div class="progress-bar">
                <div class="progress-inner" id="progress-inner"></div>
            </div>

            <div class="hint" id="hint">
                Здесь будут подсказки по раунду.
            </div>

            <!-- История сигналов -->
            <div class="history-block">
                <div class="history-title">История за сессию</div>
                <div class="history-list" id="history-list">
                    Пока ещё нет сигналов.
                </div>
            </div>
        </div>
    </div>

    <div class="hint" style="margin-top:10px;">
        1️⃣ Выбери режим и актив. 2️⃣ Укажи время экспирации. 3️⃣ Нажми <strong>«Получить прогноз»</strong> и действуй по плану.
    </div>
</div>

<script>
    const tg = window.Telegram?.WebApp;
    if (tg) {
        tg.expand();
    }

    const STORAGE_KEY = 'aisignal_state_v1';

    const btnStart = document.getElementById('btn-start');
    const selectAsset = document.getElementById('asset');
    const selectExpiry = document.getElementById('expiry');

    const signalBlock = document.getElementById('signal-block');
    const signalAsset = document.getElementById('signal-asset');
    const signalExpiry = document.getElementById('signal-expiry');
    const signalDirection = document.getElementById('signal-direction');
    const timerEl = document.getElementById('timer');
    const hintEl = document.getElementById('hint');
    const progressInner = document.getElementById('progress-inner');
    const userPill = document.getElementById('user-pill');

    const modeToggle = document.getElementById('mode-toggle');
    const modeHint = document.getElementById('mode-hint');
    const modeButtons = modeToggle.querySelectorAll('.mode-btn');

    const historyList = document.getElementById('history-list');

    let currentMode = 'calm';
    let timerId = null;
    let totalSeconds = 0;
    let remaining = 0;
    let history = [];

    // Показываем юзера в шапке
    try {
        if (tg && tg.initDataUnsafe && tg.initDataUnsafe.user) {
            const u = tg.initDataUnsafe.user;
            const name = (u.username && '@' + u.username) ||
                         [u.first_name, u.last_name].filter(Boolean).join(' ') ||
                         ('ID ' + u.id);
            userPill.textContent = name;
        } else {
            userPill.textContent = 'Локальный режим';
        }
    } catch (e) {
        userPill.textContent = 'Профиль недоступен';
    }

    // ----- Режимы -----

    function applyMode(mode, changeExpiry = true) {
        currentMode = mode;
        modeButtons.forEach(btn => {
            btn.classList.toggle('active', btn.dataset.mode === mode);
        });

        if (mode === 'calm') {
            modeHint.textContent = 'Спокойный режим — больше времени на решение, меньше шума.';
            if (changeExpiry) {
                selectExpiry.value = '3m';
            }
        } else {
            modeHint.textContent = 'Агрессивный режим — короткие сделки и максимум динамики.';
            if (changeExpiry) {
                selectExpiry.value = '15s';
            }
        }
    }

    modeButtons.forEach(btn => {
        btn.addEventListener('click', () => {
            const mode = btn.dataset.mode;
            applyMode(mode, true);
            saveState();
        });
    });

    // ----- Таймер -----

    function startTimer(seconds) {
        if (timerId) {
            clearInterval(timerId);
        }
        totalSeconds = seconds;
        remaining = seconds;
        updateTimerUI();

        timerId = setInterval(() => {
            remaining -= 1;
            if (remaining <= 0) {
                clearInterval(timerId);
                timerId = null;
                remaining = 0;
                updateTimerUI();
                hintEl.textContent = 'Раунд завершён. Проверь результат у брокера и при желании начни новый.';
                btnStart.disabled = false;
                btnStart.textContent = 'Новый раунд';
            } else {
                updateTimerUI();
            }
        }, 1000);
    }

    function updateTimerUI() {
        timerEl.textContent = remaining.toString();
        if (totalSeconds > 0) {
            const progress = ((totalSeconds - remaining) / totalSeconds) * 100;
            progressInner.style.width = progress.toFixed(0) + '%';
        } else {
            progressInner.style.width = '0%';
        }
    }

    // ----- История -----

    function addToHistory(entry) {
        history.unshift(entry);
        history = history.slice(0, 6);
        renderHistory();
    }

    function renderHistory() {
        if (history.length === 0) {
            historyList.textContent = 'Пока ещё нет сигналов.';
            return;
        }

        historyList.innerHTML = '';
        history.forEach(item => {
            const row = document.createElement('div');
            row.className = 'history-item';

            const left = document.createElement('div');
            left.className = 'history-left';

            const assetEl = document.createElement('div');
            assetEl.className = 'history-asset';
            assetEl.textContent = item.asset;

            const expiryEl = document.createElement('div');
            expiryEl.className = 'history-expiry';
            expiryEl.textContent = item.expiryLabel + ' · ' + item.modeLabel;

            left.appendChild(assetEl);
            left.appendChild(expiryEl);

            const right = document.createElement('div');
            right.className = 'history-right';

            const dirEl = document.createElement('div');
            dirEl.textContent = item.direction === 'UP' ? 'ВВЕРХ' : 'ВНИЗ';
            dirEl.className = item.direction === 'UP' ? 'history-dir-up' : 'history-dir-down';

            const timeEl = document.createElement('div');
            timeEl.className = 'history-time';
            timeEl.textContent = item.time;

            right.appendChild(dirEl);
            right.appendChild(timeEl);

            row.appendChild(left);
            row.appendChild(right);

            historyList.appendChild(row);
        });
    }

    function formatTime(date) {
        const h = String(date.getHours()).padStart(2, '0');
        const m = String(date.getMinutes()).padStart(2, '0');
        return `${h}:${m}`;
    }

    // ----- LocalStorage -----

    function saveState() {
        try {
            const state = {
                asset: selectAsset.value,
                expiryValue: selectExpiry.value,
                mode: currentMode
            };
            window.localStorage.setItem(STORAGE_KEY, JSON.stringify(state));
        } catch (e) {}
    }

    function loadState() {
        try {
            const raw = window.localStorage.getItem(STORAGE_KEY);
            if (!raw) return;
            const state = JSON.parse(raw);

            if (state.mode === 'aggressive' || state.mode === 'calm') {
                applyMode(state.mode, false);
            } else {
                applyMode('calm', false);
            }

            if (state.asset && [...selectAsset.options].some(o => o.value === state.asset)) {
                selectAsset.value = state.asset;
            }

            if (state.expiryValue && [...selectExpiry.options].some(o => o.value === state.expiryValue)) {
                selectExpiry.value = state.expiryValue;
            }
        } catch (e) {
            applyMode('calm', false);
        }
    }

    loadState(); // восстановим прошлый выбор

    // ----- Кнопка запуска -----

    btnStart.addEventListener('click', () => {
        const asset = selectAsset.value;
        const expiryOption = selectExpiry.options[selectExpiry.selectedIndex];
        const seconds = parseInt(expiryOption.dataset.seconds, 10);
        const expiryLabel = expiryOption.textContent;

        const direction = Math.random() < 0.5 ? 'UP' : 'DOWN';

        signalAsset.textContent = asset;
        signalExpiry.textContent = expiryLabel;

        const arrow = direction === 'UP' ? '↑' : '↓';
        const text = direction === 'UP' ? 'ВВЕРХ' : 'ВНИЗ';

        signalDirection.innerHTML = '';
        const arrowSpan = document.createElement('span');
        arrowSpan.className = 'arrow';
        arrowSpan.textContent = arrow;

        const textSpan = document.createElement('span');
        textSpan.textContent = text;

        signalDirection.appendChild(arrowSpan);
        signalDirection.appendChild(textSpan);

        signalDirection.className =
            'direction-value ' + (direction === 'UP' ? 'direction-up' : 'direction-down');

        signalBlock.style.display = 'block';
        btnStart.disabled = true;
        btnStart.textContent = 'Таймер идёт…';

        hintEl.textContent = 'Сигнал получен. Открой сделку у брокера и жди окончания таймера.';
        startTimer(seconds);

        const now = new Date();
        addToHistory({
            asset,
            expiryLabel,
            direction,
            modeLabel: currentMode === 'calm' ? 'спокойный' : 'агрессивный',
            time: formatTime(now)
        });

        saveState();
        // при желании сюда можно добавить tg.sendData(...)
    });
</script>
</body>
</html>
